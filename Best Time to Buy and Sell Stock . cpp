class Solution {
public:
    int maxProfit(vector<int>& prices) {
int diff;
int prof=0;
int mini =prices[0];
for(int i=1;i<prices.size();i++){
    diff= prices[i]-mini;
    prof=max(prof,diff);
    mini= min(prices[i],mini);
}
return prof;
    }
};
